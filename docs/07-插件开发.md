# 07 插件开发

NeoBot 通过 **neobot-modloader** 从文件系统目录加载 Python 插件。本文是插件开发的入门指南；**完整 API 文档见 [packages/modloader/README.md](../packages/modloader/README.md)**（747 行，含全部注册方式与示例），以下仅做概览与示例。

## 官方插件与第三方插件

| | 官方插件 | 第三方插件 |
|---|---|---|
| 位置 | `app/src/neobot_app/builtin_plugins/`（随本体分发） | 数据目录 `plugins/`（`plugins.dir`，默认 `./plugins`） |
| 安装 / 更新 / 卸载 | 不支持（随本体升级） | 支持（面板或 `PluginControlFacade`） |
| 启停 / 热重载 | 支持 | 支持 |
| 配置来源 | 本体 `config.toml` 的同名分区（由官方配置 provider 注入） | 插件自己的 `plugin.toml` 的 `[config]` |
| 受 `plugins.enabled` 影响 | 否，始终加载 | 是，`plugins.enabled = false` 时全部跳过 |

- 同名时**官方插件优先**，第三方副本被忽略。
- 启停状态持久化在数据目录 `plugin_state.json`，与 `plugin.toml` 的 `enabled` 解耦：改文件不会覆盖面板里保存的启停状态。
- 官方插件的配置不进 `plugin.toml`：本体会按插件名读取 `config.toml` 中的同名分区并注入（例如网页面板读 `[dashboard]`），`plugin.toml` 的 `[config]` 只作为读取失败时的兜底默认值。
- 第三方插件 `[config]` 可在网页面板在线编辑：键名命中 `api_key` / `token` / `password` / `secret` 等敏感规则的字段**只写不读**（面板只显示「已设置 / 未设置」，留空表示不修改）；含密钥时该插件的源码模式会被禁用。

## 插件结构

插件可以是单个 Python 文件（第三方插件）：

```text
plugins/
  ping.py
```

也可以是一个包（官方插件同样是包）：

```text
plugins/
  weather/
    plugin.toml
    __init__.py
```

插件模块必须导出一个 Plugin 实例（当前版本只保留这一种写法）：

```python
from neobot_modloader import Plugin

plugin = Plugin("ping")
```

> 不再支持旧的 setup(ctx)、ctx.on.*、Matcher、on_command() 等兼容 API。

## plugin.toml 字段

包形式的插件可带一个 `plugin.toml`（单文件插件不需要）：

```toml
name = "weather"
version = "1.0.0"
description = "天气查询插件"
author = "your-name"
dependencies = []            # 依赖的其它插件名
priority = 0                 # 加载优先级
min_neobot_version = ""      # 最低本体版本
python_dependencies = []     # 需要自动安装的第三方包

# 以下为可选元数据（更新检查与面板展示用）
repo = "https://github.com/you/neobot-plugin-weather"
branch = "main"
homepage = "https://example.com/weather"
license = "MIT"
tags = ["tool", "weather"]
hot_reload = true            # 插件本体是否支持面板内热重载（不重启进程）
config_hot_reload = true     # 插件配置改动是否支持不重启生效

[config]                     # 插件自身配置；官方插件改用本体 config.toml 的同名分区
enabled = true
```

- `repo` / `branch` 用于面板的「检查更新 / 更新此插件」（仅第三方插件）。
- `homepage`、`license`、`tags` 仅用于展示。
- `hot_reload` / `config_hot_reload` 缺省为 `true`，也可在 `Plugin(...)` 构造参数里声明（`plugin.toml` 优先）。
  面板会把两项能力分别标记为「可热重载 / 本体需重启」与「配置可热重载 / 配置需重启」；
  声明 `hot_reload = false` 后，面板与 `PluginControlFacade.reload()` 会拒绝热重载并提示重启。
  插件自身持有监听端口、后台线程或启动期快照时应当声明为 `false`（例如官方 dashboard 插件）。
- 插件下载代理可在 `[plugins]` 配置（`proxy_mode` / `proxy_host` / `proxy_port`），
  面板「插件管理 → 代理设置」可随时切换，无需重启。

## 管理接口（PluginControlFacade）

网页面板等调用方通过 `PluginControlFacade` 管理插件，方法均返回 `PluginOperationResult`（`ok` / `name` / `state` / `error` / `requires_restart` / `path`）：

| 方法 | 说明 |
|---|---|
| `load_path(path, start=True)` | 从磁盘路径加载插件 |
| `unload(name)` | 卸载（不删除文件） |
| `reload(name, start=True)` | 重新加载 |
| `start(name)` / `stop(name)` | 启动 / 停止 |
| `set_enabled(name, enabled)` | 启停并持久化到 `plugin_state.json`（官方与第三方统一处理） |
| `install(repo, branch=None, replace=False, start=True)` | 从 Git 仓库安装（仅第三方） |
| `uninstall(name)` | 卸载并删除文件（仅第三方；面板不允许卸载自身） |
| `check_update(name)` / `check_updates()` | 检查更新 |
| `snapshot()` / `get(name)` | 插件快照列表 / 单个快照（含 `official`、`manageable`、`hot_reloadable`、`hot_reload`、`config_hot_reload`） |
| `installer_proxy()` / `set_installer_proxy(mode, host, port)` | 读取 / 切换插件下载代理（system / none / custom） |
| `config_model(name)` | 插件声明的配置模型（面板据此生成官方插件配置表单） |

约束：官方插件 `manageable = False`，不能安装/更新/卸载；面板自身不能在面板内停用或重载。

## 宿主服务注册表

插件读取本体服务无需依赖装配代码，通过 `ctx.plugin_host.services` 访问：

```python
services = ctx.plugin_host.services
config = services.get("config")            # 未注册时返回 None
adapter = services.require("adapter")      # 未注册时抛 KeyError，并列出可用服务名
names = services.names()                   # 可用服务名列表
infos = services.describe()                # [{name, description, available}, ...]
```

本体在装配阶段注册的核心服务包括 `config`、`adapter`、`logger_factory`、`host_commands`、`group_queue`、`friend_queue`、`reply_orchestrator`、`emoji_service`、`tts_service`、`file_server`、`image_pool`、`drawing_manager`、`creator_image_service`、`scheduled_task_manager`、`problem_solver_manager`、`notification_hub`、`self_heal_manager`、`browser_lifecycle_manager`、`vision_detect_service`、`vision_provider`、`provider`、`command_service`、`credential_manager`、`sleep_service`、`freeze_service`、`cache_calculator` 等（以 `services.names()` 的实际结果为准）。

## 最小官方插件示例

官方插件就是 `app/src/neobot_app/builtin_plugins/` 下的一个子包，目录结构与普通插件包一致：

```text
app/src/neobot_app/builtin_plugins/
  hello/
    __init__.py
    plugin.toml
```

`plugin.toml`：

```toml
name = "hello"
version = "1.0.0"
description = "示例官方插件"
author = "NeoBot"
homepage = "https://github.com/SuperQuail/NeoBot"
license = "MIT"
tags = ["official", "example"]

# 官方插件配置来自本体 config.toml 的同名分区（这里读 [hello]），
# 本表仅作为读取失败时的兜底默认值。
[config]
enabled = true
greeting = "你好"
```

`__init__.py`：

```python
"""最小官方插件：注册一条命令与一个工具。"""

from __future__ import annotations

from neobot_modloader import Plugin, Reply

plugin = Plugin("hello", version="1.0.0", description="示例官方插件")


@plugin.on.command("hello")
async def hello() -> Reply:
    return Reply("你好！")


@plugin.on.tool("hello_echo", description="回显一段文本")
async def hello_echo(text: str) -> str:
    return text
```

要点：

- 官方插件无需登记到任何清单，放在 `builtin_plugins/` 下即会被扫描（`builtin_plugin_dirs()`）。
- 需要配置时按插件名在 `config.toml` 中新增同名分区，并在插件内通过 `ctx.config` 使用。
- 官方插件只能启停与重载，不能从面板安装/更新/卸载，改动随本体版本发布。

参考实现：[dashboard 网页面板](../app/src/neobot_app/builtin_plugins/dashboard/)。

## 注册命令

```python
from neobot_modloader import Plugin, Reply

plugin = Plugin("ping")

@plugin.on.command("ping")
async def ping():
    return Reply("pong")
```

## 注册消息处理器

```python
@plugin.on.message("你好")
async def hello(message):
    return Reply("你好呀！")
```

支持消息模式（MessagePattern）与正则模式（RegexPattern）匹配。

## 注册工具（function-calling）

```python
@plugin.on.tool("get_weather", description="查询天气", parameters={...})
async def get_weather(city: str) -> str:
    return f"{city} 的天气是晴天"
```

工具以 OpenAI function-calling 形式注入给 LLM，名称自动加前缀。

## 注册子 Agent

```python
@plugin.on.agent("analyzer", description="数据分析 Agent")
async def analyzer(task: str) -> str:
    # 返回分析结果
    return f"分析结果: {task}"
```

## Markdown Skill

在插件包内创建 skills/<skill-name>/SKILL.md，modloader 会自动加载为 Markdown Skill（见 packages/modloader/example_plugins/full_plugin/skills/weather-guide/SKILL.md 示例）。

## 钩子（生命周期事件）

插件可订阅生命周期事件：

```python
@plugin.on.lifecycle("config.changed")
async def on_config_changed():
    ...
```

## 插件数据库

插件可携带数据库迁移（database_example 示例展示了 migrations.py + models.py 的用法），通过 PluginDatabase 访问。

## 配置模型

```python
from pydantic import BaseModel

class WeatherConfig(BaseModel):
    api_key: str = ""

plugin = Plugin("weather", config=WeatherConfig)
```

## 参考示例

packages/modloader/example_plugins/ 包含：

- [ping.py](../packages/modloader/example_plugins/ping.py) — 最简单的命令插件
- [agent_workflow.py](../packages/modloader/example_plugins/agent_workflow.py) — Agent 工作流
- [vision_echo.py](../packages/modloader/example_plugins/vision_echo.py) — 视觉回显
- [database_example/](../packages/modloader/example_plugins/database_example/) — 插件数据库 + 迁移
- [full_plugin/](../packages/modloader/example_plugins/full_plugin/) — 完整插件（含 Markdown Skill）

插件系统测试见 packages/modloader/tests/（覆盖 API 契约、生命周期、数据库、工具安全与官方/第三方插件管线）。

## 下一步

- 完整 API：阅读 [packages/modloader/README.md](../packages/modloader/README.md)
- 面板中的插件管理：见 [04-功能文档/网页面板](04-功能文档/网页面板.md)
- 贡献插件：遵循 [CONTRIBUTING.md](../CONTRIBUTING.md) 流程提交 PR
