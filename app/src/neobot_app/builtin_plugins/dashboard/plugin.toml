name = "dashboard"
version = "1.0.0"
description = "NeoBot 网页面板：运行监测、日志、插件管理、本体配置在线编辑"
author = "NeoBot"
homepage = "https://github.com/SuperQuail/NeoBot"
license = "MIT"
tags = ["official", "web", "console"]

# 配置保存在插件数据目录 plugins_data/dashboard/config.toml（面板「插件」页可编辑）；
# 是否启用是 plugin_state.json 里的独立记录，不写在配置里。
# 这里只放打包默认值，留空表示使用 DashboardConfig 的默认值。
